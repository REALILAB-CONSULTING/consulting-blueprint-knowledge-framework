# Docker Desktop - guía mínima

1. Instalar Docker Desktop.
2. Levantar docs: `docker-compose -f docker-compose.yml up --build -d`
3. Usar el contenedor para ejecutar validadores y renderizar docs.
4. No incluir credenciales en imágenes; usar secrets en CI.
