version: '3.8'
services:
  docs:
    image: python:3.11-slim
    working_dir: /workspace
    volumes:
      - ./:/workspace
    command: sh -c "pip install mkdocs && mkdocs serve -a 0.0.0.0:8000"
    ports:
      - "8000:8000"